server.port=${APP_PORT:8080}
MODEL_HOST=http://localhost:${MODEL_SERVICE_PORT:8081}
management.endpoints.web.exposure.include=prometheus,health,info
management.endpoint.prometheus.enabled=true
server.port=8080